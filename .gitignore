# ---- node / next ----
node_modules/
.pnp
.pnp.js
.next/
out/
build/
dist/
.turbo/
*.tsbuildinfo
next-env.d.ts

# ---- env ----
.env
.env.*
!.env.example

# ---- logs / os ----
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store
Thumbs.db

# ---- editors ----
.vscode/*
!.vscode/extensions.json
.idea/

# ---- supabase local stack ----
supabase/.branches/
supabase/.temp/
supabase/.env

# ---- python anomaly sidecar (services/anomaly) ----
__pycache__/
*.py[cod]
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# ---- model artefacts (IsolationForest dumps, feature caches) ----
*.joblib
*.pkl
*.pickle
*.onnx
*.npy
*.npz
models/
artifacts/
mlruns/

# ---- coverage ----
coverage/
.coverage
htmlcov/

# ---- misc ----
.vercel

# Local state for `node scripts/progress.mjs`, the build dashboard. Per-machine scratch that
# would conflict on every pull, and it describes a working session rather than the project.
.halisaha-progress.json

# `npx expo export` and `next build` outputs are covered by dist/ and .next/ above; this catches
# the explicit --output-dir some of the CI invocations use.
apps/mobile/dist/
/tmp/
